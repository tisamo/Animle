export type LoginInfos = {
  name: string;
  password: string;
};

export type RegisterInfos = {
  name: string;
  password: string;
  email: string;
};

export type User = {
  name?: string;
  email?: string;
};
